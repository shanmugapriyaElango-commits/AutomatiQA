import { test, expect } from '@playwright/test';\nimport { MainPage } from '../pages/MainPage';\n\ntest.describe('Verify Partner Solution Page Navigation', () => {\n  let mainPage: MainPage;\n\n  test.beforeEach(async ({ page }) => {\n    mainPage = new MainPage(page);\n  });\n\n  test('should navigate through partner solution pages correctly', async ({ page }) => {\n    // Step 1: Navigate to Home\n    await mainPage.navigateToMain();\n\n    // Step 2: Click ServiceNow and Assert URL\n    await mainPage.clickServiceNow();\n    await expect(page).toHaveURL(/.*servicenow-testing/);\n\n    // Step 3: Click Salesforce and Assert URL\n    await mainPage.clickSalesforce();\n    await expect(page).toHaveURL(/.*salesforce/);\n\n    // Step 4: Click Snowflake and Assert URL\n    await mainPage.clickSnowflake();\n    await expect(page).toHaveURL(/.*snowflake-testing/);\n  });\n});